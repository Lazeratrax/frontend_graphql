const Base = '/app'

export const NAV = {
    App: Base,
    Posts: `${Base}/posts`,
    Post: `${Base}/posts/:id`,
    Profile: `${Base}/profile`,
    Login: '/login',
    Registration: '/registration',
    Logout: '/logout',
}
